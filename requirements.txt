# Needs Python 3.10 or newer: python-frontmatter and Markdown both use typing
# features added in 3.10. build.py checks this and says so. Note that macOS
# ships 3.9 as /usr/bin/python3, so create the venv with an explicit version:
#
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# CI builds on 3.12 (see .github/workflows/deploy-site.yml).

pyyaml>=6.0
jinja2>=3.1
markdown>=3.5
python-frontmatter>=1.1
bibtexparser>=1.4
pygments>=2.17
