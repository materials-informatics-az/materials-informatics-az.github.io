# The built site. build.py writes it from scratch on every run and the deploy
# workflow uploads it as-is; nothing in here is source.
_publish/

notes/

# Build and tooling
.venv/
__pycache__/
build.log

# macOS. This file becomes the ROOT .gitignore once site/ is promoted, so it
# has to carry the rules the old root one did.
.DS_Store